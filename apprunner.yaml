version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Using Dockerfile for build"
run:
  runtime-version: latest
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"
    - name: OPENAI_API_KEY
      value: ""  # Set this in App Runner console 