version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Using custom Dockerfile for build"
run:
  command: python3 -m uvicorn api.app:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
  env:
    - name: OPENAI_API_KEY
      value: "your-default-api-key" 