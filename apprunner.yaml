version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3 -m pip install fastapi uvicorn
run:
  runtime-version: 3.11
  command: uvicorn minimal_test:app --host 0.0.0.0 --port 8080 