version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3 -m pip install -r api/requirements.txt
run:
  runtime-version: 3.11
  command: python3 api/app.py
  network:
    port: 8000
    env: PORT
  env:
    - name: PORT
      value: "8000" 